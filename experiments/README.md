# Experiments

Use this folder for quick prototypes and research notes.

Good first experiments:

- OCR label extraction on generated diagrams.
- Arrow endpoint detection.
- Circle and line detection for mechanical callouts.
- Bar chart value extraction.
- Overlay generation from findings JSON.

Keep experiments small and promote stable code into `mcp-server/` when a workflow becomes repeatable.
